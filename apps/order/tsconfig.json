{
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": "tsconfig/pkgs.json",
    "compilerOptions": {
        "rootDir": "src",
        "outDir": "build"
    },
    "include": ["src/**/*.json", "src/**/*.ts"],
    "exclude": ["node_modules", "build"]
}
